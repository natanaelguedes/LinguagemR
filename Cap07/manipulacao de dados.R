#o que fazemos essencialmente durante  a etapa de manipulação dos dados?
#cada variável em uma coluna
#cada observação em uma linha

#como a linguagem R pode ajudar o cientista de dados?
#usando dois pacotes dplyr e tidyr

#dplyr = select(), filter(),group_by(),summarise(),arrange(),join(), mutate()
#tidyr - gather(),spread(),separate(),unite()

#tidyr = remodelagem de dados - converter uma linha numa coluna

$#gather() transforma linha em colunas
  #spread() converte colunas em linhas
    #separate() dia , mes e ano
  # unite() Une várias colunas em uma colando strings
  # tidyr é um função tem objetivo transformar dados(shape) e isso pode fazer muita diferença no processo de análise.
  #select parte da porção daqueles dados
  #aplicar o filtro utilizando alguma regra
  #group_by agrupa os dados
  #sumarise() sumarizar os dados ou seja resumir
  #arrange()  reorganizar linhas e colunas
  #mutate() realizar cálculo dentro de uma mesma coluna
  #join usado para unir dois ou mais datasets